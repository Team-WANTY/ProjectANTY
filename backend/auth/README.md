# To run
Install [uv](https://docs.astral.sh/uv/getting-started/installation/). 

Run `uv sync --dev` to update packages, then `uv run uvicorn service.views:auth_router --reload` to test the service.
