services:
  traefik:
    command:
      - "--api.dashboard=true"
      - "--providers.docker=true"
      - "--entrypoints.web.address=:80"
    volumes:
      - "./traefik/traefik.dev.yml:/traefik.yml:ro"
