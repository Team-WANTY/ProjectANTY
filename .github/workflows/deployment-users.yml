name: Users Service Deployment Orchestration

on:
  push:
    branches: ["main"]
    paths: ["backend/users_service/**", "backend/shared/**"]

jobs:
  deploy:
    uses: ./.github/workflows/deployment_orchestration.yml
    with:
      service: "users"
    secrets: inherit
    