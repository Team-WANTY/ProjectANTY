name: Auth Service Validation Pipelines

on:
  push:
    branches: ["main"]
    paths: ["backend/auth_service/**", "backend/shared/**"]

jobs:
  deploy:
    uses: ./.github/workflows/deployment_orchestration.yml
    with:
      service: "auth"
    secrets: inherit
    vars: inherit