name: Auth Service Deployment Orchestration

on:
  push:
    branches: ["main"]
    paths: ["backend/auth_service/**", "backend/shared/**"]

jobs:
  deploy:
    uses: ./.github/workflows/deployment_orchestration.yml
    with:
      service: "auth"
    secrets: inherit
    