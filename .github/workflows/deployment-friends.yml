name: Friends Service Deployment Orchestration

on:
  push:
    branches: ["main"]
    paths: ["backend/friends_service/**", "backend/shared/**"]

jobs:
  deploy:
    uses: ./.github/workflows/deployment_orchestration.yml
    with:
      service: "friends"
    secrets: inherit
    vars: inherit