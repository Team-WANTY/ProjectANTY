name: Build & Push Redirect Service

on:
  push:
    branches: ["main"]
    paths: ["backend/redirect_service/**"]
  workflow_dispatch:

jobs:
  build-and-push-redirect:
    name: Build and push service to Container Registry
    runs-on: ubuntu-latest
    environment: "Azure Deployment Environment"
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Azure Login via OIDC
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.CLIENT_ID }}
          tenant-id: ${{ secrets.TENANT_ID }}
          subscription-id: ${{ secrets.SUBSCRIPTION_ID }}

      - name: Set up Azure PowerShell
        uses: azure/powershell@v2

      - name: Login to ACR
        run: az acr login --name ${{ vars.ACR_NAME }}

      - name: Build and Push Redirect Service
        run: |
          docker build -f backend/redirect_service/Dockerfile \
            -t ${{ vars.ACR_URL }}/redirect-service:latest \
            backend
          docker push ${{ vars.ACR_URL }}/redirect-service:latest

      - name: Deploy to Redirect App
        run: |
          az containerapp update \
            --name redirect-service \
            --resource-group ${{ vars.AZURE_RESOURCE_GROUP }} \
            --image ${{ vars.ACR_URL }}/redirect-service:latest \
            --cpu 0.25 \
            --memory 0.5Gi \
            --min-replicas 1 \
            --max-replicas 3