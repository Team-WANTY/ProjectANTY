name: Comments Service Deployment Orchestration

on:
  push:
    branches: ["main"]
    paths: ["backend/comments_service/**", "backend/shared/**"]

jobs:
  deploy:
    uses: ./.github/workflows/deployment_orchestration.yml
    with:
      service: "comments"
    secrets: inherit
    